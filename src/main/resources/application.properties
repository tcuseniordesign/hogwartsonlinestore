# In earlier version of spring, default url will be jdbc:h2:mem:testdb and testdb was created by
# default. From Spring Boot 2.3.0 onwards, if datasource url is not mentioned it will auto generate
# database name. auto generated database name can be found in the spring logs from console.
spring.datasource.url=jdbc:h2:mem:testdb

spring.messages.basename=home, subscribe

fileUploadURL=D:/HogwartsProductImages/
